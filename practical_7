def coin_change(coins, amount):
    # Create a DP array
    # dp[i] stores the minimum coins needed to make amount i
    dp = [float('inf')] * (amount + 1)

    # Base case
    dp[0] = 0

    # Calculate minimum coins for every amount
    for i in range(1, amount + 1):
        for coin in coins:
            if coin <= i:
                dp[i] = min(dp[i], dp[i - coin] + 1)

    # If amount cannot be formed
    if dp[amount] == float('inf'):
        return -1

    return dp[amount]


# Input from user
coins = list(map(int, input("Enter coin denominations: ").split()))
amount = int(input("Enter the amount: "))

result = coin_change(coins, amount)

if result == -1:
    print("It is not possible to make the given amount.")
else:
    print("Minimum number of coins required:", result)

Enter coin denominations: 5
Enter the amount: 2000
Minimum number of coins required: 400
